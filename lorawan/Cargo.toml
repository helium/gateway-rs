[package]
name = "lorawan"
version = "0.1.0"
authors = ["Marc Nijdam <marc@nova-labs.com>"]
edition = "2021"
license = "Apache-2.0"

[dependencies]
bitfield = "0.14"
bytes = "1"

[dev-dependencies]
base64 = ">=0.21"
